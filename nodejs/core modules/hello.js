console.log("node version: ", process.version); //  apps which doesnt work because of older nodejs version can get an error with this

console.log("Platfrom: ", process.platform); //cross platfrom work with ifelse if(process.platform === "win64" || "linux")


